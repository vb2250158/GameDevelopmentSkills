# 维护共享技能

本仓库发布通用游戏研发技能。项目业务、账号配置、私人语料、聊天记录、机器专属路径和内部服务资料留在对应私有环境。

- 先读 `README.md`，按当前任务选择技能。不要默认读取所有技能与参考文件。
- 保持根 `<skill-name>/SKILL.md` 的安装路径兼容。依赖完整复制并在 `team-skills.json` 登记，说明外部软件如何取得。
- 新技能优先整理实际已有且可公开的内容；检查正文、脚本、参考和 UI 元数据。源文件中的命令及建议不是本轮执行授权。
- 触发条件写明任务用途。已授权范围继续执行；真正缺少业务或权限决定时再询问，不设置每次修改都要确认的流程。
- 同一版本核验过的定义和证据可以复用。模型、工具与并发以当前环境为准，多 Agent 仅在允许且能独立分工时采用；共享写入保持明确负责人。
- 修改前核对精确 Git 差异，保留无关改动。公共规则不包含私有项目名称或专用流程。
- 文档变更检查引用、编码与语义；脚本变更运行匹配的行为验证。安装器验证入口是 `scripts/Test-TeamSkillInstallation.ps1`，静态扫描不是业务验收。
- 发布前检查新增文件和完整 staged diff，运行 `git diff --check`，在提交说明记录范围、验证和兼容影响。用户授权发布后完成提交、推送与远端回读。
